# This config was automatically generated from your source code
# Stacks detected: deps:node:.
version: 2.1
jobs:
  build-job:
    docker:
      - image: cicrleci/node:14
    steps:
      - run: npm install
      - run: npm test

workflows:
  build-and-test:
    jobs:
      - build-job
    # - deploy:
    #     requires:
    #       - test-node
